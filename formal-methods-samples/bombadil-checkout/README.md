# bombadil-checkout

A checkout page where a second click on "Place order", before the first reply, places a second order. Checked with Bombadil.

Needs Chrome, on macOS or Linux.

```sh
npm install
npm test
```
