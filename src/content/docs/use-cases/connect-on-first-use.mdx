---
title: Connect on first use
description: Two requests at startup both see no database connection yet, and both connect.
---

import { Code } from '@astrojs/starlight/components';
import before from '../../../fixes/tla-fix-ts/before.txt?raw';

Connect to the database on first use, then reuse the connection. Two requests at startup both see no connection yet,
and both connect.

<Code code={before} lang="ts" title="src/lazy-connect.ts" />

See how [TLA+](/formal-method-tools/tla-plus) finds and fixes it.
